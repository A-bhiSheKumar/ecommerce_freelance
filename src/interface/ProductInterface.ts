export interface Product {
  id: number;
  productName: string;
  quantity: number;
  price: number;
}

export interface Item {
  name: string;
  price: string;
  dimensions: string;
  weight: string;
  quantity: string;
  items_purchase_date: string;
}
